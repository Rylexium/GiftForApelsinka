<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gift_for_apelsinka">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GiftForApelsinka"
        tools:targetApi="31">
        <service android:name=".service.GoodMorningService"
            android:enabled="true"
            android:exported="false"
            android:process=":newproc"
            />
        <service android:name=".service.RandomQuestionService"
            android:enabled="true"
            android:exported="false"
            android:process=":newproc"
            />
        <service android:name=".service.LocationService"
            android:enabled="true"
            android:exported="false"
            android:process=":newproc"
            android:foregroundServiceType="location"
            />
        <service android:name=".service.NotificationFromServerService"
            android:enabled="true"
            android:exported="false"
            android:process=":newproc"
            />
        <activity
            android:name=".activity.about.AboutActivity"
            android:exported="false" />
        <activity
            android:name=".activity.photo.PhotosActivity"
            android:exported="false" />
        <activity
            android:name=".activity.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>